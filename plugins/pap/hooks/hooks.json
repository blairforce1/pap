{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "g=\"${CLAUDE_PROJECT_DIR:-.}/scripts/guard-branch.sh\"; if [ -x \"$g\" ]; then exec \"$g\" hook; fi"
          }
        ]
      }
    ]
  }
}
