# Scratch: previews the security workflow on blairforce1/.github's change/security-scanning branch on pap. Not
# for merging.
name: security

on:
  pull_request:

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: blairforce1/.github/.github/workflows/security.yml@preview-security-scanning
